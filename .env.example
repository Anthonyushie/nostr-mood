# Nostr Wallet Connect Configuration (optional for Lightning payments)
NWC_CONNECTION_STRING=nostr+walletconnect://pubkey@relay?secret=your_secret

# Database Configuration (if using PostgreSQL instead of memory storage)
DATABASE_URL=your_postgresql_connection_string_here

# Environment
NODE_ENV=development